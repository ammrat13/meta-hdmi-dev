# Auto-generated with `bitbake-layers create-layer`

BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-hdmi-dev"
BBFILE_PATTERN_meta-hdmi-dev = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-hdmi-dev = "6"

LAYERDEPENDS_meta-hdmi-dev = "core"
LAYERSERIES_COMPAT_meta-hdmi-dev = "kirkstone langdale"
